<UserControl x:Class="Pokemon_Like.MVVM.View.SignUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pokemon_Like.MVVM.View"
             xmlns:vm="clr-namespace:Pokemon_Like.MVVM.ViewModel"
             d:DataContext="{d:DesignInstance Type=vm:SignUpVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Button Content="Sign up" Command="{Binding SignUpCommand}" Margin="150,190,600,190" Background="Aqua"/>
        <Button Content="Back to Home" Command="{Binding RequestMainView}" Margin="300,190,300,190"/>
    </Grid>
</UserControl>
