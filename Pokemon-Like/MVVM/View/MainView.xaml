<UserControl x:Class="Pokemon_Like.MVVM.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Pokemon_Like.MVVM.View"
             xmlns:vm="clr-namespace:Pokemon_Like.MVVM.ViewModel"
             d:DataContext="{d:DesignInstance Type=vm:MainViewVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>


        <Button Command="{Binding RequestLoginView}" Content="Login" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200" Height="50" Background="AliceBlue" Grid.Row="1"/>
        <Button Command="{Binding RequestSignUpView}" Content="Sign Up" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200" Height="50" Background="Aqua" Grid.Row="2" />
    </Grid>
</UserControl>
